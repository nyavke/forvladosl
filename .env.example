# Базовый URL backend-API. В дев-режиме Vite читает переменные с префиксом VITE_.
# Скопируйте этот файл в .env и при необходимости подставьте другой адрес.
VITE_API_BASE_URL=https://code-core.online/api/v2
